<!--pages/classification/classification.wxml-->
<view class="type" enable-flex>
    <side-nav sideList="{{sideList}}"
    scrollTops="{{scrollTops}}"
    rightCur="{{rightCur}}"
    leftCur="{{leftCur}}"
    ></side-nav>
    <good-list></good-list>

</view>
